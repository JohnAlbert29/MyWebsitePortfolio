{
  "name": "analytics",
  "version": "1.0.0",
  "scripts": {
    "build": "mkdir -p dist && cp *.html dist/ && cp *.js dist/",
    "start": "serve dist"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.0.0",
    "node-fetch": "^2.6.7"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}